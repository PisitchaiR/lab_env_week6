pipeline {
    agent none
    stages {
        stage('run docker'){
            steps{
                try{
                    sh 'docker ps -a'
                }
                catch(){
                    echo "Failed on run docker"
                }
            }
        }
    }
}