pipeline {
    agent none
    stages {
        stage('run docker'){
            steps{
                try{
                    sh 'docker ps -a'
                }
                catch(all){
                    echo "Failed on run docker"
                }
            }
        }
    }
}